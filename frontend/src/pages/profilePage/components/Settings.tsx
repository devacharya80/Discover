import{ArrowLeft,LogOut,ShieldCheck}from"lucide-react";import{useNavigate}from"react-router-dom";import{useGlobalContext}from"../../../context/GlobalContext";export default function Settings(){const navigate=useNavigate();const{user,logout}=useGlobalContext();return <div className="p-6"><header className="flex items-center gap-3"><button onClick={()=>navigate("/profile")} className="rounded-full p-2 hover:bg-gray-100"><ArrowLeft size={20}/></button><h1 className="text-2xl font-bold">Settings</h1></header><div className="mt-7 space-y-3"><div className="rounded-2xl border p-4"><div className="flex items-center gap-2 font-semibold"><ShieldCheck size={18}/>Account</div><p className="mt-2 text-sm text-gray-500">Signed in as {user?.email}</p><p className="mt-1 text-xs text-gray-400">Authentication uses a secure HTTP-only session cookie.</p></div><button onClick={()=>navigate("/profile/me")} className="w-full rounded-xl border p-3 text-left text-sm font-medium hover:bg-gray-50">Edit profile</button><button onClick={async()=>{await logout();navigate("/")}} className="flex w-full items-center justify-center gap-2 rounded-xl border border-red-200 p-3 text-sm font-semibold text-red-600"><LogOut size={16}/>Sign out</button></div></div>}